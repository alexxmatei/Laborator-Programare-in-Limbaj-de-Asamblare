#include <stdio.h>
#include <stdlib.h>

int main()
{
  unsigned char u_char = 5u;
  int varUtilizator = 0;
  char * string = "Un sir";
  scanf("%d", varUtilizator);
  printf("%s %u", string, u_char);
}